# dependencies
node_modules/
functions/node_modules/

# build output
dist/

# local env and secrets
.env.local
.env*.local
functions/.env
functions/.env.local

# eval runs (regenerated by npm run eval)
evals/runs/

# local LLM traces (raw prompts/responses, debugging only)
llm-traces/

# firebase
.firebase/
firebase-debug.log
functions/firebase-debug.log

# os / editor
.DS_Store
*.log

# transient Vite config timestamp files, regenerated by the dev server
vite.config.js.timestamp-*
