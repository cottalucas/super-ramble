# CLAUDE.md

Before any task in this repo, run the loop in [docs/orchestration.md](docs/orchestration.md).
Read the `docs/` set first. Build to those docs. Update them and append a
resolution-log entry when done.

The `docs/` set is the source of truth, not this file. This file only points there.
